# Publishes the website to GitHub Pages automatically on every push.
# Permanent link will be: https://tram2026.github.io/AI-for-Small-Business/
name: Deploy website to GitHub Pages

on:
  push:
    branches: ["claude/pensive-bell-Br2yA"]
  workflow_dispatch:   # allows manual "Run workflow" from the Actions tab

permissions:
  contents: read
  pages: write
  id-token: write

# Only one deploy at a time; newer pushes cancel older in-progress runs.
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Turns GitHub Pages on (build type: GitHub Actions) if not already enabled.
      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      # Uploads the whole site (these static files) as the Pages artifact.
      - name: Upload site files
        uses: actions/upload-pages-artifact@v3
        with:
          path: "."

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
